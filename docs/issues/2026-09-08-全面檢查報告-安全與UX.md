# 全面檢查報告：安全、不合理之處、使用者體驗（2026-09-08）

> 範圍：前端（index.html、js/、sw.js）、後端（firebase-functions/functions/src 35 檔）、專案結構與文件。
> 方式：只讀不改。每一項都附檔案:行號，可直接跳過去看。
> 同日已另行修好的項目：首屏載入優化、compiled.css 過期、node_modules 誤入 git、tailwind.config.js 死檔（見 commit ce1dd2d）。

---

## 0. 一頁摘要：最該先處理的 10 件事

| # | 嚴重度 | 問題 | 位置 |
|---|---|---|---|
| 1 | 高 | Session 30 天不可撤銷，「登出」只清手機端，伺服器端 token 仍有效 | `_helpers.js:70,149`、`js/app.js:227-232` |
| 2 | 高 | 員工填的「請假原因」未轉義就塞進管理員畫面（HTML 注入） | `js/admin.js:3944,4001,4033` |
| 3 | 高 | 備註/姓名經 DOMPurify 但仍可植入「假按鈕」，誘導管理員刪除任意打卡 | `js/admin.js:419-425,542-547,1185` |
| 4 | 高 | 打卡冷卻只在前端；後端無 60 秒去重、無 rate limit、無 App Check | `punch.js:110-128`、`js/punch/punch-flow.js:26-55` |
| 5 | 高 | 月初收到「昨天漏打卡」提醒，回來補卡卻被擋「不能選本月以前」 | `js/punch/make-up.js:37-54`、`checkYesterdayPunch.js:136` |
| 6 | 高 | GPS 失敗訊息三語混雜（英文原文 + code + 中文），沒說下一步 | `js/punch/geolocation.js:167-169` |
| 7 | 高 | LINE 提醒永遠中文（`languag` 拼錯，後端沒存語言）；已核准請假的日子也被催 | `js/app.js:411`、`checkYesterdayPunch.js:208-209` |
| 8 | 高 | Font Awesome 從未載入，全站 37 個 `<i class="fas">` 圖示都是空白 | `index.html`（無載入）、`compiled.css`（0 條 `.fas`） |
| 9 | 中 | 管理員代補卡端點缺驗證（type 無白名單、note 無長度、日期無範圍） | `adjustPunchAsAdmin.js:57-92`、`updateAttendanceAsAdmin.js:73-117` |
| 10 | 中 | `serviceAccountKey.json`（Firebase 管理員私鑰）放在「公開 repo」的資料夾內，只靠 .gitignore 擋 | `scripts/serviceAccountKey.json` |

---

## 1. 後端安全（Cloud Functions）

### 做對的地方（先講）
- Firestore rules 全鎖（`firestore.rules:7-9`），所有讀寫都走 Cloud Functions，授權集中一處。
- 33 個 function 中 30 個驗 session，12 個管理員端點都在後端查 `employees.dept === "管理員"`（`_helpers.js:192-199`），不信任前端傳的角色。
- 員工端 9 個可指定他人 id 的端點全部有 ownership 檢查或強制回自己 id。**未發現 IDOR。**
- 機密走 `defineSecret`，git 歷史掃描無硬編碼金鑰。
- OAuth state 後端產生、10 分鐘、交易式一次性消費。
- 審核狀態機只允許 `?` → `v/x`；未核准的補卡不計工時。

### 高
**B-H1　Session 不可撤銷**　`_helpers.js:70,149-150,365-373`、`index.js`（無 logout）、`js/app.js:227-232`
- 30 天固定有效、非滑動，沒有 logout / 撤銷 / 輪替端點。前端「登出」只清 localStorage。
- 情境：共用手機、遺失手機、或被 XSS 偷走 token，30 天內都可冒用；管理員 token 外洩 = 30 天內可改任何人出勤、核准、設薪資、讀全員 email/薪資。
- 修法：新增 `logout` action 刪 `sessions/{token}`；改滑動式 7 天閒置失效；管理員 session 更短。

### 中
**B-M1　`month` 只驗格式不驗範圍，無資料月份也寫聚合 doc**　`_helpers.js:210-212`、`_attendance.js:566-591`
- `isValidMonth` 接受 0001-01～9999-12；`records.length === 0` 仍 `tx.set`。任何登入員工可迴圈 12 萬個月份灌爆 `attendanceMonthly` 與帳單。
- 修法：限 2020-01～今日+1 月；空資料直接回空陣列不寫入。

**B-M2　後端無重複打卡防護**　`punch.js:110-128`
- 冷卻只在前端記憶體（重整即歸零）；curl 可秒寫上千筆。`deleteAttendance.js:50` 註解會誤導維護者以為後端有擋。
- 修法：寫入前查同 userId 最近一筆同 type，60 秒內拒絕；或以 `${userId}_${type}_${minuteBucket}` 為 doc id 做冪等。

**B-M3　無 rate limit、無 App Check；`getLoginUrl` 未驗證且每次寫一筆**　所有 `onCall`、`getLoginUrl.js:30-32`
- 匿名打 10 萬次 = 10 萬筆 `oauthStates`，隔天 03:00 才清。
- 修法：`enforceAppCheck: true`；`oauthStates` 改 Firestore TTL 政策。

**B-M4　redirect_uri 白名單用 `startsWith`，可被繞過**　`_helpers.js:46`
- `https://wenhuistage-ops.github.io.evil.com/` 與 `https://wenhuistage-ops.github.io@evil.com/` 都通過。目前靠 LINE 主控台的 callback 註冊擋住，但這層形同虛設。
- 修法：`new URL(url).origin === "https://wenhuistage-ops.github.io"`。

**B-M5　管理員寫入端點缺驗證**　`adjustPunchAsAdmin.js:57-62,87-89`、`updateAttendanceAsAdmin.js:73-78,95,117`、`punchWithoutLocation.js:33`
- `type` 無白名單、`datetime` 只驗 isNaN、`note` 未 clampText、`lat/lng` 可為 NaN、`timestamp` 無範圍。
- 後果：傳 `type: "請假"` + `audit: "v"` 可讓當日工時歸零；900KB note 撞 Firestore 1MiB 上限讓整月月曆 500；年份 9999 產生垃圾聚合。
- 修法：重用員工端已有的 `isValidPunchType`、`isReasonableAttendanceDate`、`clampText`、`validateCoordinates`。

### 低
- **B-L1**　OAuth state 未綁定用戶端；存下的 redirectUrl 沒被使用（`_helpers.js:439`、`getProfile.js:103-118`）。建議導入 PKCE。
- **B-L2**　`exchangeToken` 是死端點，`oneTimeTokens` 無人寫入；`getProfile` 回傳的 `sToken` 就是 30 天 token（`exchangeToken.js:28`、`_helpers.js:359-379`）。刪除或真正實作。
- **B-L3**　Session token 明文當 doc id（`_helpers.js:366-367`）。改存 sha256。
- **B-L4**　`deleteAttendance.js:69-71` 把整筆含 GPS 座標的 doc 印進 Cloud Logging；`checkYesterdayPunch.js:194,213,225` 逐人印姓名。只記 docId 與前 8 碼。
- **B-L5**　`getProfile.js:139-143` 把 LINE 原始錯誤 JSON 回給前端。固定回 `ERR_LINE_AUTH_FAILED`。
- **B-L6**　Session 快取跨容器 60 秒不失效（`_helpers.js:86-116`），降權後短暫仍有舊權限。
- **B-L7**　管理員可自審自批（`approveReview.js:26-40`、`adjustPunchAsAdmin.js:52,94`）。`targetUserId === 自己` 時拒絕。
- **B-L8**　待審清單以目標日期而非申請時間過濾，可產生永遠看不到的待審件（`getReviewRequest.js:57-61`）。
- **B-L9**　`.doc()` 拼接未做字元白名單，含 `/` 的 userId 直接 500（`getCalendarSummary.js:29-32`、`approveReview.js:17-20`）。`setEmployeeSalaryProfile.js:48-50` 已有正確寫法可複用。
- **B-L10**　地點名稱、休息時段字串無長度上限（`addLocation.js:18,29`、`setBreakTimes.js:43`）。
- **B-L11**　`submitLeave.js:76,84-86`：`type` 非白名單、`reason` 自由文字但扣薪規則靠它判斷。套用 `updateLeaveAsAdmin.js:26-29` 的 `LEAVE_KINDS`。
- **B-L12**　`cors: true` 允許任何來源。改成明確網域。
- **B-L13**　`firebase.json` predeploy lint 是 `echo 'no lint configured'`。加 ESLint。
- **B-L14**　id_token 只 base64 解碼未驗簽（`getProfile.js:67-83`），目前只用於顯示 email，無害但要標示「未驗證」。
- **B-L15**　`checkYesterdayPunch.js:208-209` 不過濾 `audit`，被退回的補卡也算「有打卡」。
- **B-L16**　`_attendance.js:32` `parseMonth` 接受 `\d{1,2}`，腳本 `--month=2026-13` 靜默算成 2027-01。
- **B-L17**　`_attendance.js:235-245` 同日同 HH:MM/type 第二筆被靜默丟棄，UI 無法刪除它。

---

## 2. 前端安全

### 做對的地方
- CSP `script-src` 無 `'unsafe-inline'`/`'unsafe-eval'`，index.html 零 inline script、零 `on*=`。
- 三個 CDN 資源 SRI 雜湊經實際下載比對全部正確；DOMPurify 自託管且與官方位元組一致。
- OAuth state 前端 + 後端雙驗；管理員 UI 每次回伺服器 `checkSession` 驗，`localStorage.userDept` 只當顯示快取。
- Service Worker 只攔同源 GET、不快取 OAuth 回跳、API 不經 SW，快取內沒有含 session 的回應。

### 高
**F-H1　員工「請假原因」未轉義直接寫入管理員請假統計卡片**　`js/admin.js:3944 → 4001 → 4033`
- 全專案唯一含使用者資料卻沒過 DOMPurify 的 innerHTML。前端用 `<select>` 但 API 任填（`make-up.js:608-615`），後端 `clampText` 只截長度不轉義。
- 情境：員工送 `<style>…</style>` 或釣魚 `<a>`，審核清單看起來只是怪文字，核准後統計卡片原樣渲染。CSP 擋 script，但 `style-src 'unsafe-inline'` 讓 CSS 注入、版面覆蓋、釣魚連結成立。
- 修法：共用 `escapeHtml()` 對所有 `${使用者欄位}` 轉義；再包 `DOMPurify.sanitize`。

**F-H2　DOMPurify 保留 class/data-*，員工可在管理員畫面植入真的會被綁定的按鈕**　`js/admin.js:419-425,542-604,1145-1196,4133-4170`
- 事件用 class 委派（`.admin-delete-record-btn`、`.approve-btn`），`data-doc-id` 直接取自 dataset。
- 情境：員工在 `adjustPunch.note` 放 `<button class="admin-delete-record-btn" data-doc-id="別人的打卡id">刪除</button>`，管理員看到多一顆「刪除」，確認框文字也是攻擊者的 dataset 組出來的，按下即刪任意出勤。
- 修法：同 F-H1 轉義；委派 handler 只認程式產生的節點（例如 `btn.closest('[data-actions-root]')`）。

### 中
- **F-M1**　登出只清 localStorage（`js/app.js:227-232`），同 B-H1。
- **F-M2**　`?action=in|out` 連結會在無確認下自動打卡（`js/punch/auto-punch.js:20-48`），且 `checkAutoPunch` 被呼叫兩次（`js/app.js:457,468`）。任何人把 `?action=out` 貼給同事，對方一開就被打下班。修法：加確認框；刪 `app.js:457` 那次呼叫。
- **F-M3**　打卡冷卻只在前端（同 B-M2）。
- **F-M4**　補卡日期限制只在前端（當月、不晚於今日），後端接受 2020-01-01～今天+400 天（`make-up.js:37-54` vs `_helpers.js:220-226`）。
- **F-M5**　CSP 以整個 `cdnjs.cloudflare.com` 為信任來源（已知 CSP-bypass gadget 寄存地）；`placehold.co` 只為兩個預設頭像。改路徑級白名單、頭像改本地。
- **F-M6**　薪資日別（國定/例假/休息）完全取決於第三方假日 API `api.pin-yi.me`，快取在可改寫的 localStorage（`holidays-client.js:20,47-54`、`labor-hours.js:232-234`）。API 錯或停擺 → Excel 薪資靜默算錯。改由後端提供或內建官方假日表交叉比對。

### 低
- **F-L1**　`(0, eval)(txt)` 仍在 `js/admin.js:1800-1816`（不在 docs 說的 weekly-chart.js），正式環境不執行且被 CSP 擋，但 `?_ensure=` 時間戳會污染 SW 快取。整段刪除。
- **F-L2**　14 處 innerHTML 未過 DOMPurify（目前只含常數/i18n，安全但無防護）。統一政策。
- **F-L3**　地點名稱未消毒塞進 Leaflet popup（`js/location.js:73-74`）。
- **F-L4**　console.log 印 LINE userId 與整筆紀錄（`abnormal-records.js:76`、`admin.js:40-43,316`、`ui.js:598,612`）；全站 81 個 console.log。
- **F-L5**　`navigator.userAgent` 被當打卡備註存進人資紀錄並顯示給管理員（`punch-flow.js:88,220`、`make-up.js:517,532,556`）。
- **F-L6**　z18 圖磚請求間接把員工位置（約 140m 解析度）告知 OpenStreetMap。
- **F-L7**　無 framebusting（meta CSP 不支援 `frame-ancestors`）。在 theme-init.js 加 `window.top !== window.self` 檢查。
- **F-L8**　`window.open(LINE_URL, '_blank')` 未加 `noopener`（`js/app.js:507`）。
- **F-L9**　無 `<meta name="referrer">`。
- **F-L10**　`res.user.picture || res.user.rate` 用不存在的欄位當頭像 fallback（`js/app.js:52,56`），日後若加 `rate`（時薪）會寫進 `<img src>`。
- **F-L11**　載入未使用的 Firestore SDK（`firestore-client.js:48,53`）；Firebase apiKey 應在 GCP 加 HTTP referrer 限制。
- **F-L12**　死碼：`js/modules/request-approval.js`（用 `prompt`，無人呼叫）、`js/modules/state.js` 的 AppState（無人呼叫）。
- **F-L13**　正式 UI 保留測試按鈕 `test-notification-btn`（`index.html:942`）、`test-api-btn`（`:1303`）。

---

## 3. 使用者體驗

### 做對的地方
- 定位權限引導彈窗依 iOS/Android/桌機分別說明、有「重新嘗試」、全走 i18n（`geolocation.js:228-306`）。
- 防重複打卡三層：60 秒冷卻 + 進行中 flag + 成功後立即追加到今日紀錄。
- 破壞性操作一律自製 `showConfirmDialog` 且標明是哪一筆；開關失敗自動回滾。
- 5 語系 616 個 key 完全同步，前端使用的 key 無缺漏。
- 補卡表單預填 08:00/18:00、病假證明軟提醒不強制、照片前端壓到 700KB。

### 高
**U-H1　月初依提醒回來補上月最後一天是死路**　`make-up.js:37-54`、`ui.js:772-785`、`checkYesterdayPunch.js:136`
- 提醒 → 切上月點昨天 → 填好送出才跳「不能選本月以前」，沒說該怎麼辦。舞台場次常在月底，這條路徑很常走到。
- 修法：下限改「今天往前 N 天」；月曆層就不顯示不可補的日期；`datetime-local` 加 `min/max`。

**U-H2　GPS 失敗訊息三語混雜、沒有下一步**　`geolocation.js:167-169`、`location.js:256`
- 越南員工看到「Không thể lấy vị trí: Timeout expired [code 3] (已重試 1 次)」。已有 `ERROR_GEOLOCATION_TIMEOUT/UNAVAILABLE/PERMISSION_DENIED` 5 語 key 卻只在 `location.js:233-245` 用到。
- 超出範圍、精確度差的訊息也沒有「請走近『{地點}』再試」這類可執行建議。
- 修法：依 `err.code` 映射到既有 key；訊息加一句動作。

**U-H3　打卡 API 無逾時，弱網下按鈕可卡「處理中…」最長 70 秒**　`firestore-client.js:107-108`、`punch-flow.js:58-60`
- 修法：`httpsCallable(fn, action, { timeout: 15000 })`，逾時走既有 `ERR_FIRESTORE_CALL_FAILED` 文案（已 5 語）。

**U-H4　所有 info 型通知都顯示成紅色錯誤**　`core.js:135-141`
- 只認 success/warning，其餘一律 `bg-red-500`。從 LINE 一鍵打卡第一眼是紅色「正在自動打卡…」；GPS 精確度提示紅色讓員工以為失敗而再按。
- 修法：補 `info` 樣式。

**U-H5　Session 使用中過期沒有全域攔截，介面默默失效**　`firestore-client.js:79-125`、`core.js:52-60`
- 打卡紅 toast、月曆停在「正在載入…」、異常清單變整月異常，畫面仍顯示已登入。文案「Session 已過期」外籍員工看不懂。
- 修法：`callApifetch` 統一攔截 `ERR_SESSION_EXPIRED/INVALID` → 清 token、顯示「請重新登入」。

**U-H6　LINE 提醒永遠中文；請假的人也被催**　`js/app.js:411`（`languag` 拼錯）、`getProfile.js`（不讀語言）、`_helpers.js:350`（`preferredLanguage: ""` 之後無人更新）、`checkYesterdayPunch.js:208-209`
- 六語文案形同虛設；有請假紀錄的日子判成「漏打上班卡」；假日/休息日也發（每週一早上全員收到）。
- 修法：`getProfile/checkSession` 存 `currentLang`；提醒略過請假日、國定假日、例假日。

**U-H7　Font Awesome 從未載入，全站圖示空白**　`index.html`（無載入）、`compiled.css`（0 條 `.fas`）、37 處 `<i class="fas …">`
- 修法：改用 inline SVG 或 emoji（不要為此再加一個 CDN）。

### 中
- **U-M1**　審核結果不通知員工；「員工報表」路徑退回不問原因（`approveReview.js`/`rejectReview.js` 無 push；`admin.js:4194-4195` vs `1272-1277`）。
- **U-M2**　凌晨 00:00–08:00 點月曆「今天」沒反應（`ui.js:416-419` 用 `new Date('YYYY-MM-DD')` 以 UTC 解析）。`ui.js:780-781` 已有正確寫法。
- **U-M3**　「補全日」拆兩次 API，下班失敗重送會重複上班申請（`make-up.js:509-546`）。
- **U-M4**　假別以翻譯後文字存進資料庫（`make-up.js:375-377,412-415`），同一種病假存成「病假 / Nghỉ ốm / Izin Sakit」，統計拆成多類。改固定代碼。
- **U-M5**　打卡主按鈕被 256px 地圖擠出手機首屏（`index.html:421-446`）；按錯上/下班無確認（`app.js:235-236`）。
- **U-M6**　已送出申請的日子仍以紅字列在異常紀錄，「審核中」硬編中文（`abnormal-records.js:49-56,257,264-268`）。
- **U-M7**　病假證明 `capture="environment"` 在 Android 強制開相機，選不到相簿（`make-up.js:268`）。拿掉 `capture`。
- **U-M8**　對員工顯示技術性錯誤字串：`my-requests.js:59-60,76`、`app.js:394`（「state mismatch」）、`app.js:418`；後端 28 個 code 在 5 語系無翻譯（`ERR_NOT_REVIEWABLE`、`ERR_NOT_FOUND`、`ERR_BELOW_MIN_WAGE`、薪資表單 7 個 `ERR_INVALID_*` 等）。
- **U-M9**　月曆載入失敗永遠停在「正在載入…」（`ui.js:49,69-75`）。改「載入失敗，點此重試」。
- **U-M10**　語言切換器在登入後才看得到（`index.html:337`）；`state.js:39` 讓瀏覽器語言偵測永遠不執行；`html.lang` 從不更新。
- **U-M11**　`checkAutoPunch` 呼叫兩次（`app.js:457,468`），第二次撞 flag 跳警告。
- **U-M12**　Modal 焦點管理缺失、Esc 幾乎無效（`#confirm-dialog` 無 role/tabindex，`core.js:207-216` 綁 keydown 但從未 focus）。
- **U-M13**　確認框內 `\n` 全被壓平（`index.html:1323-1325` 無 `whitespace-pre-line`），離職五點清單變一行。
- **U-M14**　沒有全域載入指示；`loadingId` 指向不存在的 `#loading`（`firestore-client.js:91-92`）。
- **U-M15**　toast 無 `aria-live`，3–8 秒消失，錯誤不留痕（`index.html:1315-1317`）。

### 低
- **U-L1**　觸控面積小於 44px：月份 ◄/►、篩選 chip、修改/刪除、核准/拒絕、休息時段 ✕。
- **U-L2**　對比與深色模式漏網：異常日/待處理日對比約 2.6–2.8:1（`style.css:229-237`）；41 處 `text-gray-500/600` 無 `dark:`；定位引導卡與 PWA 橫幅硬編白底。
- **U-L3**　月曆日期格是 `div` + click 委派，鍵盤與讀屏無法操作（`ui.js:299`）。
- **U-L4**　`#language-switcher`、`#theme-toggle` 無 `aria-label`。
- **U-L5**　殘留硬編中文：`make-up.js:579,638`「提交中...」、`ui.js:686`「小時」、`admin.js:1577-1619,1918-1923,421,1145,409`；vi `STATUS_VACATION_*` 用「Nghỉ mát」（出遊）而非「Nghỉ phép」。
- **U-L6**　`text-[10px]`（`index.html:1077,1082`）、11px（`weekly-chart.js:255-259`）。
- **U-L7**　儀表板內嵌與 modal 表單共用 `#adjustInTime/#leaveReason` id，送出可能讀到另一張；請假原因預設即「病假」無「請選擇」。
- **U-L8**　確認框「確認」永遠紅色，補卡/核准也紅；殘留原生 `alert()`（`admin.js:2575,2596,2659,2663`、`location.js:387,402`）與 `prompt`（`admin.js:1273`）。
- **U-L9**　PWA 安裝提示登入前 1.5 秒就跳，關一次永久消失。改首次打卡成功後提示。
- **U-L10**　管理員 6 tab + 5 子分頁 + 3 sub-tab 三層同樣式；分頁無 history，手機返回鍵直接離開。
- **U-L11**　管理員 LINE 通知：`submitLeave.js:119` 申請時間是 UTC `toISOString()`；`adjustPunchAsAdmin.js:115-123` 會通知到操作者自己；月底洗版。
- **U-L12**　審核只能單筆 + 固定 500ms + 整批重抓（`admin.js:1304-1306`）；月份切換無競態保護（`ui.js:26-77`）。

### 2026-07-02 報告追蹤
已修 22 項（#1–7、9–11、13–15、17–19、31、36、38–39、43、68、70、92）。
**未修 22 項**：#8/23（假別存翻譯字串）、#16/41（無逾時）、#20（按錯類型無確認）、#21（雙呼叫）、#22（GPS 混語）、#24（相同 id）、#25（凌晨點今天）、#26（過去月份補卡按鈕）、#27（補全日兩次呼叫）、#37（Font Awesome）、#40（session 無攔截）、#42（月曆失敗卡住）、#71–73、#75–77、#79–81。

---

## 4. 不合理之處與技術債（專案結構）

| 項目 | 位置 | 說明 | 建議 |
|---|---|---|---|
| Firebase 管理員私鑰放在公開 repo 資料夾內 | `scripts/serviceAccountKey.json` | 只靠 `.gitignore` 擋；一次 `git add -f` 或改錯 .gitignore 就外洩到 PUBLIC repo | 移到 `~/.config/wenhui/`，腳本改讀環境變數 `GOOGLE_APPLICATION_CREDENTIALS` |
| README 描述的是已下線的 GAS 架構 | `README.md:40-87` | 說後端是 Google Apps Script + 試算表；實際已全走 Firestore（`js/config.js:24`） | 重寫簡介與安裝章節 |
| 舊版 Apps Script 死碼 136KB | `GS/`（10 檔） | GAS 後端已下線，前端已鎖死不能切回 | 刪除或移到 `docs/legacy/` |
| 一次性遷移腳本 | `scripts/migrate-to-firestore.js`、`scripts/MIGRATE.md` | 遷移已完成 | 刪除 |
| 未使用的模板 | `templates/ui-components.html` | index.html 註解自承「模板機制未被使用」 | 刪除 |
| 死碼 JS 仍每頁載入 | `js/modules/state.js`（AppState）、`js/modules/request-approval.js` | grep 無人呼叫 | 從 index.html 移除並刪檔 |
| Vite 建置設定未用於部署 | `vite.config.js`、`dist/`、`@vitejs/plugin-legacy`、`terser` | 部署是直接 push 根目錄；`preserveScriptsPlugin` 是 no-op | 刪 vite 相關或真的改用 dist 部署（二選一） |
| 授權宣告不一致 | `package.json` `"license": "ISC"` vs `LICENSE`/README GPLv2 | | 改 `GPL-2.0-or-later` |
| package.json 描述是 badge、repo 指向舊專案 | `package.json:4,13-19` | | 清理 |
| 104 個翻譯 key 無人使用 | `i18n/*.json`（×5 語） | 約 35KB 死資料 | 刪除 |
| 薪資計算核心函式無測試 | `js/labor-hours.js`：`calcWorkHours`、`aggregateMonthLaborStats`、`monthlyToHourly`、`inferGradeFromSalary`、`calcEmployeeDeductions` | 這些直接影響 Excel 上的金額 | 各補一組已知輸入/輸出的測試 |
| 勞基法常數散落 | `js/admin.js`（35 處）、`js/labor-hours.js`（14 處） | 28590、12.5%、5.17%、1.34/1.67/2.67 前端兩檔各寫一份 | 集中到 labor-hours.js 一個常數物件 |
| 前端 `laborInsuranceGrade` 允許 1–23 但表只有 11 級 | `admin.js:2216` vs `labor-hours.js:391-403` | 後端需驗 | 後端白名單 |
| `firebase.json` predeploy lint 是假的 | `firebase.json:13-15` | | 加 ESLint |
| 81 個 console.log | `js/` | 部分印 userId 與整筆紀錄 | 包 DEBUG flag |
| 本機 commit 尚未 push | `main` ahead of origin by 1 | 今日的效能修正還沒上線 | `git push origin main` |

---

## 5. 建議處理順序

1. **一天內**：B-M4（改一行）、U-M11/F-M2 雙呼叫（刪一行）、U-H4 info 樣式、U-M7 拿掉 capture、U-M13 加 `whitespace-pre-line`、私鑰移出 repo 資料夾、push。
2. **一週內**：B-H1 + F-M1 logout 端點與滑動 TTL；F-H1/F-H2 共用 `escapeHtml()`；B-M2 後端 60 秒去重；B-M5 管理員端驗證；U-H6 語言接線 + 提醒略過請假日；U-H3 API 逾時；U-H5 session 過期攔截。
3. **一個月內**：B-M1/B-M3 範圍限制與 App Check；U-H1 補卡日期視窗；U-H2 GPS 訊息；U-H7 圖示；U-M4 假別代碼化；F-M6 假日表後端化；補薪資函式測試；清理死碼與 README。

---

## 6. 處理進度

### 2026-09-08 已修（「一天內」那組）
- **B-M4** redirect 白名單改比對 `new URL(url).origin`，並以 11 組案例驗證（含 `github.io.evil.com`、`github.io@evil.com`）。**需重新部署 Cloud Functions 才生效。**
- **U-M11 / F-M2** 刪掉 `app.js` 既有 session 路徑多出來的那次 `checkAutoPunch()`，只保留登入成功後的共用那次。
- **U-H4** 補 `info` 藍色樣式。同時發現真正的根因：`style.css` 的 `.notification` 白底灰字寫在 Tailwind layer 外，優先權高過所有 `bg-*-500`，**原本成功/警告/錯誤全部顯示成白色**。已包進 `@layer components`，瀏覽器實測四種顏色皆正確。
- **U-M7** 病假證明 `<input type="file">` 拿掉 `capture="environment"`，Android 可選相簿。
- **U-M13** `#confirm-message` 加 `whitespace-pre-line`，確認框訊息的換行會顯示。

### 2026-09-08 已修（B 的前兩項）
- **B-H1 / F-M1 登出**：新增 Cloud Function `logout`（`src/logout.js` → `_helpers.revokeSession`），刪 `sessions/{token}` 並讓本容器快取立即失效；前端登出改為先呼叫 `logout` 再清 localStorage。跨容器快取最多再撐 60 秒（既有取捨）。Session TTL 仍為固定 30 天，滑動式有效期另議。**需重新部署 Cloud Functions。**
- **U-H6 LINE 提醒語言**：修掉 `app.js` 的 `languag` 拼錯；改由 `checkSession` 帶 `language`，後端只在變動時寫入 `employees.preferredLanguage`；切換語言時立即同步；`getBroadcastText` 補 `en-US → en` 對應。另補：昨天有請假/休假申請（核准或待審）者不再被催。假日刻意不略過：後端沒有假日資料，且本行假日常上班，略過會漏掉真正忘記打卡的人。

### 2026-09-08 已修（F-H1 / F-H2）
- `js/core.js` 新增 `escapeHtml()`；`admin.js`（27 處）、`my-requests.js`（5）、`location.js`（4）、`ui.js`（2）所有把備註、請假原因、姓名、退回原因、地點名稱、Nominatim 結果拼進 innerHTML 的地方一律先轉義，DOMPurify 保留為第二層；請假統計卡片補上 DOMPurify。
- 瀏覽器實測：備註內的 `<button class="admin-delete-record-btn">`、`<style>`、`<a>` 全部變成純文字，只剩程式自己產生的按鈕。
- 刪除無人呼叫且含注入點的 `js/modules/request-approval.js`（F-L12 一併處理）；`ui-component-generator.js` 的 label 改 textContent。
- 新增 `tests/escape-html.test.js`（直接抽 core.js 的函式本體測）。
- 未做：事件委派只認程式產生節點（F-H2 的第二層）。理由：合法按鈕與使用者文字在同一段 innerHTML 樣板裡，事後無法區分；轉義已從根因解決。

### 2026-09-09 已修（第 4 節「不合理之處與技術債」清理）

專案結構與文件的清理，**未動 `js/`、`index.html`、`i18n/`、`firebase-functions/`**。

| 項目 | 處理 |
|---|---|
| README 描述已下線的 GAS 架構 | 重寫 `README.md`：簡介、架構圖、技術棧、專案結構、開發與部署章節全部改成「純靜態前端 + Cloud Functions + Firestore」。保留 GPLv2 授權與作者標註段落。 |
| 授權宣告不一致 | `package.json` `"license"` ISC → `GPL-2.0-or-later`（與 `LICENSE`、README 一致）。 |
| package.json 描述是 badge、repo 指向舊專案 | `description` 改成真實描述；`repository`/`bugs`/`homepage` 改指 `wenhuistage-ops/wenhuistage-ops.github.io`；補 `author`、`keywords`；移除指向不存在檔案的 `"main": "index.js"`。 |
| 舊版 Apps Script 死碼 136KB | `GS/` 10 檔**移到 `docs/legacy/`**（非刪除），並新增 `docs/legacy/README.md` 說明「2026 年停用、僅供查閱、不可部署」。保留理由見下方註記。 |
| 一次性遷移腳本 | 刪除 `scripts/migrate-to-firestore.js`、`scripts/MIGRATE.md`、`scripts/package.json`、`scripts/package-lock.json`。 |
| 未使用的模板 | 刪除 `templates/`（`ui-components.html` + `README.md`）。 |
| Vite 建置設定未用於部署 | 採方案 (a) 全移除：刪 `vite.config.js` 與 `dist/`；`package.json` 移除 `dev`/`build`/`preview` script 與 `vite`、`@vitejs/plugin-legacy`、`terser` 三個 devDependencies。README 明寫「沒有 build step，push 什麼線上就是什麼」。 |
| 薪資計算核心函式無測試 | `tests/labor-hours.test.js` 補 70 個測試，涵蓋 `calcWorkHours`、`calcWorkHoursFromShifts`、`aggregateMonthLaborStats`、`monthlyToHourly`、`inferGradeFromSalary`、`calcEmployeeDeductions`，以及未 export 的 `_toMinutes`／`_overlapMinutes`（經公開 API 覆蓋）。全檔 160 測試綠。 |
| docs 目錄樹過期 | `docs/README.md` 重寫成只列實際存在的檔案；`docs/architecture/專案架構.md` 架構圖、目錄樹、技術棧、API 清單全面更新（含 36 個 Cloud Function 分類表）。 |
| `docs/部署檢查清單.md` 是 PR #2 舊清單 | 改寫成通用版：前後端部署差異、上線前檢查、煙霧測試、log 檢查、rollback（含「Cloud Functions 沒有一鍵回滾」與資料面不可逆的提醒）。 |
| `docs/issues/問題分析.md` 狀態過期 | 檔頭指向本報告；2.1 的阻塞條件「等 GAS 退場」標記為已達成；2.2 移入「已解決」；9.1 因 `apiUrl` 已移除而降級改寫。 |

**GS/ 為何選擇移動而非刪除**：`firebase-functions/functions/src/` 有約 24 個檔案的檔頭註解寫
「對應 GS：Handlers.gs …」，`firebase-functions/README.md` 也以路徑指向 `GS/*.gs`；
更關鍵的是 `_attendance.js:155,364` 留有兩個**尚未完成**的 TODO，明確把 GAS 版當待對齊的規格。
在那兩個 TODO 收掉前刪除會讓維護者無從比對。詳見 `docs/legacy/README.md`。

**驗證**：`npx jest` 6 suites / 160 tests 全綠；`package.json` 經 `JSON.parse` 驗證合法；
移除 vite 相依後 `npm run build:css`（Tailwind CLI）確認仍可正常產出。
所有刪除前都以 grep 全專案（含 `js/`、`index.html`、`docs/`、`firebase-functions/`、`scripts/deploy.sh`）確認無引用。

### 2026-09-09 全面修復（本報告其餘所有項目）

分五組平行處理，每組負責不相交的檔案，最後統一驗證與收尾。

**後端 Cloud Functions（20 項）** — commit「後端：稽核報告 20 項修復」
B-M1 月份範圍與空資料不寫聚合、B-M2 後端 60 秒去重（只比對即時打卡，不誤擋補卡）、B-M5 管理員端輸入驗證、B-L2 刪除死端點 `exchangeToken`、B-L3 session token 改存 sha256（保留明文查詢作為舊 session 相容路徑，既有登入者不會被登出）、B-L4 日誌不再印 GPS 與姓名、B-L5 不回傳 LINE 原始錯誤、B-L7 禁止自審自批、B-L8 待審清單過濾、B-L9 doc id 白名單、B-L10 字串長度上限、B-L11 假別白名單（固定代碼、中文與五語字串都收）、B-L12 CORS 改明確白名單（30 個端點）、B-L13 導入真正的 ESLint、B-L15 過濾被退回的補卡、B-L16 `parseMonth` 收緊、B-L17 重複紀錄改保留並可刪、U-M1 審核結果依員工語言推播、U-L11 通知時間格式與排除操作者、勞保級距白名單對齊實際的 11 級。
另補 B-L1：`getProfile` 的 redirect_uri 改以 `getLoginUrl` 當時記錄在 state 上的值為準。

**打卡流程（12 項）**
U-H1 補卡回溯視窗改為 45 天並套 `min`/`max`、U-H2 GPS 錯誤改用既有五語 key 並附可執行建議、U-H3 API 逾時、U-H5 session 失效統一攔截、U-M3 補全日的下班失敗不再重複送出上班、U-M6 待審日改用琥珀色與「待審核」、U-M14 頂端進度條、U-M12 確認框焦點與 Esc、F-M2 一鍵打卡連結加確認、U-L7 請假原因加「請選擇」、F-L5 userAgent 縮成裝置標記、U-L5 硬編中文。
逾時後續調整為分級：打卡 15 秒（員工在等回饋），其餘 30 秒。Cloud Functions 冷啟動本身可能吃掉 5~15 秒，一律 15 秒會讓登入在冷啟動時直接失敗。

**管理端與月曆（12 項）**
U-M2 UTC 日期解析（凌晨點「今天」沒反應）、U-M8 錯誤碼統一轉譯、U-M9 月曆失敗可重試、U-M10 瀏覽器語言偵測與 `html.lang`、U-M12 modal 無障礙、U-L12 空狀態與競態保護、U-L3 月曆鍵盤操作、U-L5 硬編中文、U-L8 移除原生 `alert`/`prompt`、F-L1 移除間接 eval、勞基法常數集中（數值零改動，85 個薪資測試全綠）、U-M4 管理端假別代碼對應。

**版面、無障礙與多語（22 項）**
U-H7 載入 Font Awesome 並補 CSP `font-src`（21 個圖示恢復）、U-M5 打卡按鈕移到首屏、U-M8 補齊 59 個後端錯誤碼的五語翻譯、U-M10 登入頁語言切換、U-M12/U-M15 對話框與 toast 無障礙、U-L1 觸控面積 44px、U-L2 對比與深色模式（亮/深色皆 0 處低對比）、U-L4 aria-label、U-L6 字級、U-L7 label 關聯、U-L8 確認鍵依操作類型上色、U-L9 PWA 提示改為首次打卡成功後、U-L13 文案與 header 整理、F-M5 頭像改內嵌 SVG 並移除 `placehold.co`、F-L7 framebusting、F-L9 referrer、F-L10 頭像 fallback、F-L13 移除測試按鈕、vi 假別誤譯修正、刪除 28 個確認未使用的翻譯 key。
`sw.js` 版本由 wh-v3 升到 wh-v4，淘汰舊的 runtime 快取。

**技術債與文件（11 項）** — 見上方「2026-09-09 技術債清理」

**跨檔案項目（最後統一處理）**
- **私鑰移出 repo**：`scripts/serviceAccountKey.json` → `~/.config/wenhui/`（權限 600）。**確認從未被 commit 進 git，無外洩，不需換發**。
- **F-L4 完成**：新增 `debugLog()`（`js/core.js`），78 個 `console.log` 全部改走除錯閘門，正式網域下 `DEBUG_ENABLED === false` 完全靜音；17 個「模組已載入」雜訊直接移除；`console.error`/`warn` 保留。開啟方式：`localStorage.setItem('debug','1')`，localhost 自動開啟。
- **U-M4 前端**：`make-up.js` 的假別 `<option value>` 改為固定代碼，7 個代碼皆驗證對得上後端白名單。
- **U-L10**：`switchTab` 支援瀏覽器歷史，手機返回鍵回到上一個分頁而非離開 App；同時補上元素不存在時的防護。
- **F-M6**：新增 Cloud Function `getHolidays`，國定假日改由後端統一抓取並快取於 Firestore（全公司共用、來源失聯時回傳上次資料），前端改優先走它，第三方僅作為退路。
- **i18n 合併**：五組要求的 30 個新 key 補齊五語，全站 652 個 key 五語完全一致、無空值。

### 驗證

- 前端 26 個檔案語法全過；以 jsdom 依 `index.html` 實際順序載入，載入期與 `DOMContentLoaded` 皆零錯誤；共享作用域下跨檔全域皆可見。
- 後端 35 個檔案 `node --check` 全過、`index.js` 34 個 export 全部載入、ESLint 零錯誤。
- `npx jest`：9 個套件、264 個測試全綠（修復前為 107 個）。
- 五語系檔各 652 key、集合一致、無空值。
- 版面組另以 Chrome headless + CDP 實測：零 CSP 違規、零 console error、圖示 21/21 顯示、打卡按鈕在 390×667 首屏內、亮/深色皆 0 處低對比、無水平溢出。

### 尚未處理

- **App Check**（B-M3 的一部分）：需先在 Firebase Console 註冊 App Check provider、前端接上 `initializeAppCheck`，並確認 metrics 100% 通過後才可開 `enforceAppCheck`。現在開會讓整個系統打不開。
- **薪資計算仍在前端**：後端已擋重複打卡與輸入驗證，但管理員匯出的 Excel 數值由瀏覽器算出。要完全防竄改需把薪資計算移到後端，屬架構級改動。
- **觸控面積**：仍有 32 個元素小於 44px（分頁 pill 36px 等）。分頁樣式由 JS `classList.replace` 切換，改高度有連帶風險。
- **月曆假日紅字對比**：`.day-cell.holiday-text` 疊在各狀態底色上最差約 1.7:1，要合格需整組配色重新設計。
- **跨日班工時**：`labor-hours.js` 目前回 0，已在測試中釘為已知限制。
